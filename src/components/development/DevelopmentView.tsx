import React, { useEffect, useState } from 'react';
import { createPortal } from 'react-dom';
import { Archive, Copy, Eye, Play, Plus, Send, Trash2, X } from 'lucide-react';
import { developmentApi } from '../../api/sharedRepository';
import { useApp } from '../../context/AppContext';

type CapsuleStatus = 'BORRADOR' | 'PUBLICADA' | 'ARCHIVADA';
type Capsule = { id:string; title:string; description:string; campaignId?:string; gap:string; duration:number; status:CapsuleStatus; content:{type:'URL'|'EMBED'|'HTML'|'VIDEO';value:string}; evaluation:{type:'FORMULARIO'|'FORO';questionType?:string;prompt:string;options?:string[];correctAnswer?:string;score:number;minimumScore:number;attempts:number;feedback:string;minChars?:number;requiredResponse?:boolean;allowPeers?:boolean;allowComments?:boolean;moderation?:boolean;dueAt?:string}; required:boolean; availableAt?:string; dueAt?:string; completionCriterion:string };
type Assignment = { id:string; capsuleId:string; advisorId:string; campaignId?:string; origin:string; originId?:string; gap:string; assignedAt:string; dueAt?:string; status:'PENDIENTE'|'EN_CURSO'|'COMPLETADA'|'VENCIDA'; progress:number; result?:number; attempts:number; duration:number; forumPost?:string; evidence?:string; contentViewed?:boolean };
const emptyCapsule = ():Omit<Capsule,'id'|'status'> => ({ title:'',description:'',campaignId:'',gap:'',duration:10,content:{type:'URL',value:''},evaluation:{type:'FORMULARIO',questionType:'UNICA',prompt:'',options:[],correctAnswer:'',score:100,minimumScore:80,attempts:1,feedback:'',requiredResponse:true},required:true,availableAt:'',dueAt:'',completionCriterion:'CONTENIDO_Y_EVALUACION' });
const statusLabel:Record<string,string>={BORRADOR:'Borrador',PUBLICADA:'Publicada',ARCHIVADA:'Archivada',PENDIENTE:'Pendiente',EN_CURSO:'En curso',COMPLETADA:'Completada',VENCIDA:'Vencida'};
const sanitize = (html:string) => { const doc=new DOMParser().parseFromString(html,'text/html'); doc.querySelectorAll('script,object,embed,link,meta,base').forEach(node=>node.remove()); doc.querySelectorAll('*').forEach(node=>[...node.attributes].forEach(attr=>{if(attr.name.startsWith('on')||['srcdoc','formaction'].includes(attr.name))node.removeAttribute(attr.name); if(['href','src'].includes(attr.name)&&!/^https?:|^data:image\//i.test(attr.value))node.removeAttribute(attr.name);})); return `<!doctype html><html><body style="margin:0;background:#071426;color:#eef6ff;font-family:Arial">${doc.body.innerHTML}</body></html>`; };
const embedUrl=(value:string)=>{const match=value.match(/<(?:iframe|embed)\b[^>]*\bsrc\s*=\s*["']([^"']+)["']/i);const raw=(match?.[1]||value).trim();try{const url=new URL(raw);return url.protocol==='https:'?url.href:'';}catch{return '';}};
const containsEmbed=(value:string)=>/<(?:iframe|embed)\b[^>]*\bsrc\s*=/i.test(value);
const trustedInteractiveHost=(url:string)=>{try{return ['view.genially.com','www.canva.com'].includes(new URL(url).hostname);}catch{return false;}};

const ContentFrame:React.FC<{capsule:Capsule;onEvent?:(data:any)=>void}>=({capsule,onEvent})=>{const embedded=containsEmbed(capsule.content.value);const url=embedUrl(capsule.content.value);const trusted=trustedInteractiveHost(url);useEffect(()=>{if(!url||!onEvent)return;const origin=new URL(url).origin;const listener=(event:MessageEvent)=>{if(event.origin!==origin||!['CONTENT_COMPLETED','SCORE','ATTEMPTS','DURATION'].includes(event.data?.type))return;onEvent(event.data);};window.addEventListener('message',listener);return()=>window.removeEventListener('message',listener);},[url,onEvent]);if(capsule.content.type==='HTML'&&!embedded)return <iframe title={capsule.title} srcDoc={sanitize(capsule.content.value)} sandbox="allow-forms allow-popups" className="h-full min-h-[420px] w-full rounded-xl border border-[var(--cm-border)]"/>;if(!url)return <div className="grid min-h-[320px] place-items-center rounded-xl border border-dashed border-[var(--cm-border)] text-sm text-[var(--cm-text-secondary)]">Ingresa una URL HTTPS o código embed válido.</div>;return <iframe title={capsule.title} src={url} sandbox={trusted?undefined:'allow-scripts allow-same-origin allow-forms allow-popups allow-popups-to-escape-sandbox allow-presentation allow-downloads'} allow="fullscreen; autoplay; clipboard-read; clipboard-write; encrypted-media" allowFullScreen referrerPolicy="strict-origin-when-cross-origin" className="aspect-video min-h-[420px] w-full rounded-xl border border-[var(--cm-border)] bg-black"/>;};

export const DevelopmentView:React.FC=()=>{const{currentUser,advisors,campaigns}=useApp();const admin=currentUser.role==='ADMINISTRADOR';const[tabs,setTab]=useState(admin?'PANEL':'PENDIENTES');const[capsules,setCapsules]=useState<Capsule[]>([]);const[assignments,setAssignments]=useState<Assignment[]>([]);const[editing,setEditing]=useState<Capsule|null>(null);const[creating,setCreating]=useState(false);const[assigning,setAssigning]=useState<Capsule|null>(null);const[active,setActive]=useState<Assignment|null>(null);const load=async()=>{const[c,a]=await Promise.all([developmentApi.capsules(),developmentApi.assignments()]);setCapsules(c.capsules);setAssignments(a.assignments);};useEffect(()=>{void load();},[]);const myAssignments=assignments.filter(a=>!currentUser.advisorId||a.advisorId===currentUser.advisorId);const counts=(s:string)=>myAssignments.filter(a=>a.status===s).length;const cardCapsule=(a:Assignment)=>capsules.find(c=>c.id===a.capsuleId);const patchCapsule=async(c:Capsule,data:any)=>{await developmentApi.updateCapsule(c.id,data);await load();};
const advisorTabs=['PENDIENTES','EN_CURSO','COMPLETADA','VENCIDA'];return <main className="cm-workspace min-h-full p-5 lg:p-8"><div className="mx-auto max-w-[1500px]"><header className="cm-page-heading flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-[var(--cm-primary)]">{admin?'Gestión de Desarrollo':'Mi Desarrollo'}</p><h1 className="mt-1 text-2xl font-bold">Cápsulas de desarrollo</h1></div>{admin&&<button onClick={()=>setCreating(true)} className="cm-button-primary px-4 py-2"><Plus className="h-4 w-4"/>Crear cápsula</button>}</header><nav className="cm-tabs mt-5 flex gap-2">{(admin?['PANEL','CÁPSULAS','ASIGNACIONES','SEGUIMIENTO']:advisorTabs).map(t=><button key={t} onClick={()=>setTab(t)} className={tabs===t?'active':''}>{admin?t[0]+t.slice(1).toLowerCase():`${statusLabel[t]} (${counts(t)})`}</button>)}</nav>
{admin&&tabs==='PANEL'&&<section className="mt-5 grid gap-3 sm:grid-cols-3"><Metric label="Cápsulas" value={capsules.length}/><Metric label="Publicadas" value={capsules.filter(c=>c.status==='PUBLICADA').length}/><Metric label="Asignaciones activas" value={assignments.filter(a=>['PENDIENTE','EN_CURSO'].includes(a.status)).length}/></section>}
{admin&&tabs==='CÁPSULAS'&&<section className="mt-5 grid gap-3 lg:grid-cols-2 xl:grid-cols-3">{capsules.map(c=><article key={c.id} className="cm-card p-4"><div className="flex justify-between gap-3"><div><span className="cm-badge">{statusLabel[c.status]}</span><h3 className="mt-2 font-bold">{c.title}</h3><p className="mt-1 text-xs text-[var(--cm-text-secondary)]">{c.description||'Sin descripción'} · {c.duration} min</p></div><button onClick={()=>setEditing(c)} className="cm-button-secondary h-9 p-2"><Eye className="h-4 w-4"/></button></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>setEditing(c)} className="cm-button-secondary px-2 py-1 text-xs">Editar / previsualizar</button><button onClick={()=>void developmentApi.duplicateCapsule(c.id).then(load)} className="cm-button-secondary p-1.5"><Copy className="h-4 w-4"/></button>{c.status==='BORRADOR'&&<button onClick={()=>void patchCapsule(c,{status:'PUBLICADA'})} className="cm-button-primary px-2 py-1 text-xs">Publicar</button>}{c.status==='PUBLICADA'&&<><button onClick={()=>setAssigning(c)} className="cm-button-primary px-2 py-1 text-xs"><Send className="h-3 w-3"/>Asignar</button><button onClick={()=>void patchCapsule(c,{status:'ARCHIVADA'})} className="cm-button-secondary p-1.5"><Archive className="h-4 w-4"/></button></>}<button onClick={()=>{if(confirm(`¿Eliminar ${c.title}?`))void developmentApi.removeCapsule(c.id).then(load)}} className="cm-button-secondary p-1.5 text-[var(--cm-danger)]" title="Eliminar"><Trash2 className="h-4 w-4"/></button></div></article>)}{!capsules.length&&<Empty text="Aún no hay cápsulas creadas."/>}</section>}
{admin&&['ASIGNACIONES','SEGUIMIENTO'].includes(tabs)&&<Tracking assignments={assignments} capsules={capsules} advisors={advisors} campaigns={campaigns}/>} {!admin&&<section className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{myAssignments.filter(a=>a.status===tabs).map(a=>{const c=cardCapsule(a);return c&&<article key={a.id} className="cm-card p-4"><span className="cm-badge">{statusLabel[a.status]}</span><h3 className="mt-3 font-bold">{c.title}</h3><p className="mt-1 text-xs text-[var(--cm-text-secondary)]">{c.evaluation.type==='FORMULARIO'?'Formulario':'Foro'} · {a.origin} · {c.duration} min</p><p className="mt-2 text-xs">Vence: {a.dueAt||'Sin vencimiento'}</p><div className="mt-3 h-2 overflow-hidden rounded bg-[var(--cm-surface-elevated)]"><span className="block h-full bg-[var(--cm-primary)]" style={{width:`${a.progress||0}%`}}/></div><button onClick={()=>setActive(a)} className="cm-button-primary mt-4 w-full px-3 py-2"><Play className="h-4 w-4"/>{a.status==='PENDIENTE'?'Comenzar':a.status==='EN_CURSO'?'Continuar':'Ver resultado'}</button></article>})}{!myAssignments.some(a=>a.status===tabs)&&<Empty text="No tienes cápsulas en este estado."/>}</section>}</div>
{(creating||editing)&&<CapsuleModal initial={editing} campaigns={campaigns} onClose={()=>{setCreating(false);setEditing(null)}} onSave={async data=>{editing?await developmentApi.updateCapsule(editing.id,data):await developmentApi.createCapsule(data);setCreating(false);setEditing(null);await load();}}/>}{assigning&&<AssignModal capsule={assigning} advisors={advisors} campaigns={campaigns} onClose={()=>setAssigning(null)} onSave={async data=>{await developmentApi.assign({capsuleId:assigning.id,...data});setAssigning(null);await load();}}/>}{active&&<Player assignment={active} capsule={cardCapsule(active)!} onClose={()=>setActive(null)} onSave={async data=>{const result=await developmentApi.updateAssignment(active.id,data);setActive(result.assignment);await load();}}/>}</main>};

const Metric=({label,value}:{label:string;value:number})=><article className="cm-card p-4"><p className="text-xs text-[var(--cm-text-secondary)]">{label}</p><strong className="mt-1 block text-3xl">{value}</strong></article>;const Empty=({text}:{text:string})=><div className="cm-card col-span-full p-10 text-center text-sm text-[var(--cm-text-secondary)]">{text}</div>;
const Overlay=({children}:{children:React.ReactNode})=>createPortal(<div className="fixed inset-0 z-[200] flex items-center justify-center overflow-y-auto bg-slate-950/65 p-4 backdrop-blur-sm">{children}</div>,document.body);
const CapsuleModal=({initial,campaigns,onClose,onSave}:any)=>{const[step,setStep]=useState(1);const[form,setForm]=useState<any>(initial||emptyCapsule());const set=(key:string,value:any)=>setForm((f:any)=>({...f,[key]:value}));return <Overlay><div className="cm-modal my-auto w-full max-w-5xl"><header className="flex justify-between border-b border-[var(--cm-border)] p-5"><div><h2 className="font-bold">{initial?'Editar':'Crear'} cápsula</h2><p className="text-xs text-[var(--cm-text-secondary)]">Paso {step} de 3 · Contenido, evaluación y reglas</p></div><button onClick={onClose}><X/></button></header><div className="max-h-[75vh] overflow-y-auto p-5">{step===1&&<div className="grid gap-3 md:grid-cols-2"><Field label="Título"><input className="cm-input p-2" value={form.title} onChange={e=>set('title',e.target.value)}/></Field><Field label="Campaña"><select className="cm-select p-2" value={form.campaignId} onChange={e=>set('campaignId',e.target.value)}><option value="">Todas</option>{campaigns.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Descripción"><input className="cm-input p-2" value={form.description} onChange={e=>set('description',e.target.value)}/></Field><Field label="Competencia / brecha"><input className="cm-input p-2" value={form.gap} onChange={e=>set('gap',e.target.value)}/></Field><Field label="Duración estimada"><input type="number" className="cm-input p-2" value={form.duration} onChange={e=>set('duration',Number(e.target.value))}/></Field><Field label="Tipo de contenido"><select className="cm-select p-2" value={form.content.type} onChange={e=>set('content',{...form.content,type:e.target.value})}><option>URL</option><option>EMBED</option><option>HTML</option><option>VIDEO</option></select></Field><label className="md:col-span-2 text-sm font-semibold">URL / embed / HTML<textarea className="cm-input mt-1 min-h-28 p-2" value={form.content.value} onChange={e=>set('content',{...form.content,value:e.target.value})}/></label><div className="md:col-span-2 min-h-[430px]"><ContentFrame capsule={{...form,id:'preview',status:'BORRADOR'}}/></div></div>}{step===2&&<EvaluationEditor form={form} set={set}/>} {step===3&&<div className="grid gap-3 md:grid-cols-2"><Field label="Obligatoriedad"><select className="cm-select p-2" value={String(form.required)} onChange={e=>set('required',e.target.value==='true')}><option value="true">Obligatoria</option><option value="false">Opcional</option></select></Field><Field label="Disponibilidad"><input type="datetime-local" className="cm-input p-2" value={form.availableAt} onChange={e=>set('availableAt',e.target.value)}/></Field><Field label="Vencimiento"><input type="datetime-local" className="cm-input p-2" value={form.dueAt} onChange={e=>set('dueAt',e.target.value)}/></Field><Field label="Criterio de finalización"><select className="cm-select p-2" value={form.completionCriterion} onChange={e=>set('completionCriterion',e.target.value)}><option value="CONTENIDO_Y_EVALUACION">Contenido visto + evaluación aprobada</option><option value="CONTENIDO_Y_PARTICIPACION">Contenido visto + participación publicada</option></select></Field></div>}</div><footer className="flex justify-between border-t border-[var(--cm-border)] p-4"><button onClick={()=>step===1?onClose():setStep(step-1)} className="cm-button-secondary px-3 py-2">{step===1?'Cancelar':'Atrás'}</button>{step<3?<button onClick={()=>setStep(step+1)} className="cm-button-primary px-4 py-2">Continuar</button>:<button onClick={()=>void onSave(form)} disabled={!form.title||!form.content.value||!form.evaluation.prompt} className="cm-button-primary px-4 py-2">Guardar cápsula</button>}</footer></div></Overlay>};
const EvaluationEditor=({form,set}:any)=>{const ev=form.evaluation;const update=(x:any)=>set('evaluation',{...ev,...x});return <div className="grid gap-3 md:grid-cols-2"><Field label="Tipo"><select className="cm-select p-2" value={ev.type} onChange={e=>update({type:e.target.value})}><option value="FORMULARIO">Formulario</option><option value="FORO">Foro</option></select></Field>{ev.type==='FORMULARIO'?<><Field label="Tipo de pregunta"><select className="cm-select p-2" value={ev.questionType} onChange={e=>update({questionType:e.target.value})}><option value="UNICA">Selección única</option><option value="MULTIPLE">Selección múltiple</option><option value="VF">Verdadero / falso</option><option value="CORTA">Respuesta corta</option><option value="ABIERTA">Respuesta abierta</option></select></Field><Field label="Pregunta"><input className="cm-input p-2" value={ev.prompt} onChange={e=>update({prompt:e.target.value})}/></Field><Field label="Opciones separadas por coma"><input className="cm-input p-2" value={(ev.options||[]).join(',')} onChange={e=>update({options:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></Field><Field label="Respuesta correcta"><input className="cm-input p-2" value={ev.correctAnswer} onChange={e=>update({correctAnswer:e.target.value})}/></Field><Field label="Puntaje"><input type="number" className="cm-input p-2" value={ev.score} onChange={e=>update({score:Number(e.target.value)})}/></Field><Field label="Nota mínima"><input type="number" className="cm-input p-2" value={ev.minimumScore} onChange={e=>update({minimumScore:Number(e.target.value)})}/></Field><Field label="Intentos"><input type="number" className="cm-input p-2" value={ev.attempts} onChange={e=>update({attempts:Number(e.target.value)})}/></Field><Field label="Feedback posterior"><input className="cm-input p-2" value={ev.feedback} onChange={e=>update({feedback:e.target.value})}/></Field></>:<><Field label="Pregunta detonadora"><input className="cm-input p-2" value={ev.prompt} onChange={e=>update({prompt:e.target.value})}/></Field><Field label="Mínimo de caracteres"><input type="number" className="cm-input p-2" value={ev.minChars||20} onChange={e=>update({minChars:Number(e.target.value)})}/></Field>{['requiredResponse','allowPeers','allowComments','moderation'].map(k=><label key={k} className="flex items-center gap-2 text-sm"><input type="checkbox" checked={Boolean(ev[k])} onChange={e=>update({[k]:e.target.checked})}/>{k==='requiredResponse'?'Respuesta obligatoria':k==='allowPeers'?'Permitir respuestas de compañeros':k==='allowComments'?'Permitir comentarios':'Moderación'}</label>)}<Field label="Fecha límite"><input type="date" className="cm-input p-2" value={ev.dueAt||''} onChange={e=>update({dueAt:e.target.value})}/></Field></>}</div>};
const Field=({label,children}:{label:string;children:React.ReactNode})=><label className="flex flex-col gap-1 text-sm font-semibold">{label}{children}</label>;
const AssignModal=({capsule,advisors,campaigns,onClose,onSave}:any)=>{const[selected,setSelected]=useState<string[]>([]);const[campaignId,setCampaign]=useState('');const[groupId,setGroup]=useState('');const[origin,setOrigin]=useState('Manual');const[originId,setOriginId]=useState('');const[dueAt,setDue]=useState('');const availableAdvisors=advisors.filter((a:any)=>a.active!==false&&a.status!=='INACTIVO'&&(!campaignId||a.campaignId===campaignId)).sort((a:any,b:any)=>a.name.localeCompare(b.name));const groups=[...new Set(availableAdvisors.map((a:any)=>a.teamId).filter(Boolean))];return <Overlay><div className="cm-modal w-full max-w-xl p-5"><div className="flex justify-between"><h2 className="font-bold">Asignar · {capsule.title}</h2><button onClick={onClose}><X/></button></div><div className="mt-4 space-y-3"><Field label="Campaña (asigna a todos)"><select className="cm-select p-2" value={campaignId} onChange={e=>{setCampaign(e.target.value);setGroup('');setSelected([])}}><option value="">Sin asignación masiva</option>{campaigns.filter((c:any)=>c.status==='ACTIVA').map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Grupo / equipo"><select className="cm-select p-2" value={groupId} onChange={e=>setGroup(e.target.value)}><option value="">Sin grupo</option>{groups.map((id:any)=><option key={id} value={id}>{id}</option>)}</select></Field><Field label={`Asesores de dotación (${availableAdvisors.length})`}><select multiple className="cm-select min-h-32 p-2" value={selected} onChange={e=>setSelected([...e.target.selectedOptions].map(o=>o.value))}>{availableAdvisors.map((a:any)=><option key={a.id} value={a.id}>{a.name}</option>)}</select></Field><Field label="Origen"><select className="cm-select p-2" value={origin} onChange={e=>setOrigin(e.target.value)}>{['Manual','Evaluación de Calidad','Mejora Continua','Feedback','Plan de Acción','OJT'].map(x=><option key={x}>{x}</option>)}</select></Field>{origin!=='Manual'&&<Field label="ID del registro de origen"><input className="cm-input p-2" value={originId} onChange={e=>setOriginId(e.target.value)}/></Field>}<Field label="Vencimiento"><input type="date" className="cm-input p-2" value={dueAt} onChange={e=>setDue(e.target.value)}/></Field></div><div className="mt-5 flex justify-end gap-2"><button onClick={onClose} className="cm-button-secondary px-3 py-2">Cancelar</button><button onClick={()=>void onSave({advisorIds:selected,campaignId,groupId,origin,originId,dueAt})} className="cm-button-primary px-3 py-2">Asignar</button></div></div></Overlay>};
const Tracking=({assignments,capsules,advisors,campaigns}:any)=>{const[filters,setFilters]=useState({campaign:'',capsule:'',gap:'',state:'',from:'',to:''});const[selected,setSelected]=useState<any>(null);const set=(key:string,value:string)=>setFilters(f=>({...f,[key]:value}));const filtered=assignments.filter((a:any)=>(!filters.state||a.status===filters.state)&&(!filters.campaign||a.campaignId===filters.campaign)&&(!filters.capsule||a.capsuleId===filters.capsule)&&(!filters.gap||a.gap?.toLowerCase().includes(filters.gap.toLowerCase()))&&(!filters.from||a.assignedAt>=filters.from)&&(!filters.to||a.assignedAt.slice(0,10)<=filters.to));return <section className="cm-card mt-5 overflow-hidden"><div className="grid gap-2 p-3 md:grid-cols-3 xl:grid-cols-6"><select className="cm-select p-2" value={filters.campaign} onChange={e=>set('campaign',e.target.value)}><option value="">Campaña</option>{campaigns.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select className="cm-select p-2" value={filters.capsule} onChange={e=>set('capsule',e.target.value)}><option value="">Cápsula</option>{capsules.map((c:any)=><option key={c.id} value={c.id}>{c.title}</option>)}</select><input className="cm-input p-2" placeholder="Brecha" value={filters.gap} onChange={e=>set('gap',e.target.value)}/><select className="cm-select p-2" value={filters.state} onChange={e=>set('state',e.target.value)}><option value="">Estado</option>{['PENDIENTE','EN_CURSO','COMPLETADA','VENCIDA'].map(s=><option key={s} value={s}>{statusLabel[s]}</option>)}</select><input type="date" className="cm-input p-2" value={filters.from} onChange={e=>set('from',e.target.value)}/><input type="date" className="cm-input p-2" value={filters.to} onChange={e=>set('to',e.target.value)}/></div><div className="overflow-x-auto"><table className="cm-table min-w-[900px]"><thead><tr><th>Asesor</th><th>Cápsula</th><th>Campaña</th><th>Origen</th><th>Estado</th><th>Avance</th><th>Resultado</th><th>Vencimiento</th></tr></thead><tbody>{filtered.map((a:any)=><tr key={a.id} onClick={()=>setSelected(a)} className="cursor-pointer"><td>{advisors.find((x:any)=>x.id===a.advisorId)?.name||a.advisorId}</td><td>{capsules.find((x:any)=>x.id===a.capsuleId)?.title||a.capsuleId}</td><td>{campaigns.find((x:any)=>x.id===a.campaignId)?.name||'—'}</td><td>{a.origin}</td><td>{statusLabel[a.status]}</td><td>{a.progress}%</td><td>{a.result??'—'}</td><td>{a.dueAt||'—'}</td></tr>)}</tbody></table></div>{!filtered.length&&<Empty text="Sin asignaciones para mostrar."/>}{selected&&<Overlay><div className="cm-modal w-full max-w-lg p-5"><div className="flex justify-between"><h3 className="font-bold">Detalle de seguimiento</h3><button onClick={()=>setSelected(null)}><X/></button></div><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><Info label="Progreso" value={`${selected.progress}%`}/><Info label="Tiempo" value={`${selected.duration||0} min`}/><Info label="Intentos" value={selected.attempts||0}/><Info label="Resultado" value={selected.result??'—'}/><Info label="Participación" value={selected.forumPost||'—'}/><Info label="Evidencia" value={selected.evidence||'—'}/><Info label="Origen" value={`${selected.origin}${selected.originId?` · ${selected.originId}`:''}`}/></div></div></Overlay>}</section>};
const Info=({label,value}:{label:string;value:any})=><div className="rounded-lg bg-[var(--cm-surface-elevated)] p-3"><span className="block text-xs text-[var(--cm-text-secondary)]">{label}</span><strong>{value}</strong></div>;
const Player=({assignment,capsule,onClose,onSave}:any)=>{const[phase,setPhase]=useState(assignment.contentViewed?'EVALUACION':'CONTENIDO');const[answer,setAnswer]=useState(assignment.forumPost||'');const multiple=capsule.evaluation.questionType==='MULTIPLE';const selected=answer.split(',').map((x:string)=>x.trim()).filter(Boolean);const choose=(option:string)=>setAnswer(multiple?(selected.includes(option)?selected.filter((x:string)=>x!==option):[...selected,option]).join(','):option);return <Overlay><div className="cm-modal my-auto flex max-h-[92vh] w-full max-w-6xl flex-col"><header className="flex items-center justify-between border-b border-[var(--cm-border)] p-4"><div><h2 className="font-bold">{capsule.title}</h2><p className="text-xs text-[var(--cm-text-secondary)]">{phase==='CONTENIDO'?'Contenido':'Evaluación'} · {assignment.progress}%</p></div><button onClick={onClose}><X/></button></header><div className="min-h-0 flex-1 overflow-y-auto p-4">{phase==='CONTENIDO'?<ContentFrame capsule={capsule} onEvent={(event:any)=>void onSave(event.type==='CONTENT_COMPLETED'?{contentViewed:true}:event.type==='DURATION'?{duration:event.value}:{})}/>:<div className="mx-auto max-w-2xl"><h3 className="text-lg font-bold">{capsule.evaluation.prompt}</h3>{capsule.evaluation.type==='FORMULARIO'&&['UNICA','MULTIPLE','VF'].includes(capsule.evaluation.questionType||'')?(capsule.evaluation.questionType==='VF'?['Verdadero','Falso']:capsule.evaluation.options||[]).map(option=><label key={option} className="cm-card mt-3 flex gap-2 p-3"><input type={multiple?'checkbox':'radio'} name="answer" checked={selected.includes(option)} onChange={()=>choose(option)}/>{option}</label>):<textarea className="cm-input mt-4 min-h-40 p-3" value={answer} onChange={e=>setAnswer(e.target.value)} placeholder={capsule.evaluation.type==='FORO'?'Publica tu participación…':'Escribe tu respuesta…'}/>} {assignment.status==='COMPLETADA'&&<div className="cm-card mt-5 p-4">Resultado: {assignment.result??'Completada'} · Intentos: {assignment.attempts}</div>}</div>}</div><footer className="flex justify-end border-t border-[var(--cm-border)] p-4">{phase==='CONTENIDO'?<button onClick={()=>{setPhase('EVALUACION');void onSave({contentViewed:true});}} className="cm-button-primary px-4 py-2">Continuar a evaluación</button>:assignment.status!=='COMPLETADA'&&<button onClick={()=>void onSave({answer})} className="cm-button-primary px-4 py-2">Enviar respuesta</button>}</footer></div></Overlay>};
